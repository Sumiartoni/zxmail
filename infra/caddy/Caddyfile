{
	email {$LETSENCRYPT_EMAIL}
}

{$DASHBOARD_HOST:localhost}, localhost {
	encode gzip zstd

	@backend path /api/* /health /health/* /webhooks/*
	handle @backend {
		reverse_proxy backend:8080
	}

	handle {
		reverse_proxy frontend:3000
	}
}

{$DASHBOARD_HOST:app.zxmail.site} {
	encode gzip zstd

	@backend path /api/* /health /health/* /webhooks/*
	handle @backend {
		reverse_proxy backend:8080
	}

	handle {
		reverse_proxy frontend:3000
	}
}

{$API_HOST:api.zxmail.site} {
	encode gzip zstd
	reverse_proxy backend:8080
}

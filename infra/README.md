# Infra Layout

- `caddy/`: reverse proxy template for the frontend and backend entrypoints.
- `postal/`: starter configuration for the single-node Postal deployment used in Production v1.

This folder is intentionally limited to Docker Compose era infrastructure. No Kubernetes, Helm, Terraform, or node orchestration is included here.
